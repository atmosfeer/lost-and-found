<h1>Lost and found pets</h1>

<ul>
  <% @pets.each do |pet| %>
    <li>
      <%= link_to pet_path(pet) do %>
        <h3><%= "#{pet.name} | #{pet.category}" %></h3>
      <% end %>
    </li>
  <% end %>
</ul>
